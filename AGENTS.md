# Desktop Pet 프로젝트 작업 지침

항상 한국어로 답변한다.

## 프로젝트 목표

Windows에서 동작하는 저메모리 Desktop Pet을 만든다. 사용자가 직접 제작한 캐릭터가 화면 위를 자유롭게 돌아다니고, 클릭·더블클릭·드래그 같은 상호작용에 반응하며, 사용하지 않을 때는 가중치 기반 행동 상태 머신으로 자율 행동한다. 타이머·포모도로 같은 작은 유틸리티 기능도 캐릭터 행동과 연결한다.

상세 기준과 현재 결정은 `PROJECT.md`를 우선 확인한다.

## 우선순위

1. 메모리와 유휴 CPU 사용량을 낮게 유지한다.
2. Windows 네이티브 동작과 안정성을 우선한다.
3. 캐릭터 행동이 기계적인 완전 랜덤이 아니라 자연스럽게 이어지게 한다.
4. 기능을 작은 단위로 구현하고 각 단계마다 실제 동작을 검증한다.
5. 새 기능보다 기존 동작의 안정성과 확장 가능한 구조를 우선한다.

## 기술 방향

- 기본 언어: Rust
- 대상 OS: Windows
- UI/윈도우: Win32 API 직접 사용
- 투명 Pet 창: Layered Window 기반
- 캐릭터: PNG 스프라이트 애니메이션
- 행동: Weighted State Machine
- 웹 UI/WebView/Electron은 기본적으로 사용하지 않는다.
- 외부 라이브러리는 꼭 필요한 경우에만 추가하고, 추가 전 메모리·배포 크기·유지보수 영향을 검토한다.

## 작업 규칙

- 기존 구조와 네이밍을 유지하며 요청 밖 리팩터링을 하지 않는다.
- 한 단계에서 하나의 핵심 동작을 완성하고 검증한다.
- Windows API의 unsafe 코드는 가능한 좁은 모듈에 격리한다.
- 행동 로직, 렌더링, 윈도우 이벤트, 유틸리티 기능을 서로 강하게 결합하지 않는다.
- 캐릭터별 데이터와 애니메이션은 코드와 분리한다.
- 캐릭터를 추가하기 위해 핵심 엔진 코드를 수정해야 하는 구조를 피한다.
- 설정과 상태 파일은 사람이 읽을 수 있는 간단한 형식을 우선한다.
- 고정 60 FPS 루프를 기본값으로 두지 않는다. 애니메이션이나 이동이 필요한 때만 필요한 주기로 갱신한다.
- 모든 스프라이트를 무조건 선로딩하지 않는다. 실제 메모리 사용량 측정 결과를 기준으로 캐시 정책을 결정한다.
- 다중 모니터와 DPI 환경을 고려한다.
- 화면 밖으로 캐릭터가 영구적으로 사라지는 상태를 만들지 않는다.
- 투명 픽셀 영역은 가능한 경우 아래 창으로 입력을 통과시킨다.
- 커밋은 사용자가 명시적으로 요청했을 때만 한다.
- 커밋/문서/주석에 AI 작성자·기여자 메타 정보를 남기지 않는다.

## 검증

변경 후 가능한 가장 작은 검증부터 실행한다.

- 빌드 성공
- 실행/종료 정상 동작
- Pet 창 표시 여부
- 입력 처리
- 화면 경계 처리
- 해당 단계에서 추가한 상태 전이
- 유휴 CPU/메모리 사용량 확인

성능 목표는 임의의 숫자로 확정하지 않는다. 첫 네이티브 프로토타입의 실제 측정값을 기준선으로 잡고 이후 변경이 이를 악화시키는지 비교한다.

## 구현 순서

`PROJECT.md`의 단계별 계획을 따른다. 현재 단계가 끝나기 전에 뒤 단계 기능을 한꺼번에 넣지 않는다.
